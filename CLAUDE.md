# 全局个人规则

以下规则适用于任何项目，不只是 bigOS；项目专属规则（架构约束、构建约定等）应该维护在各项目自己的 AGENTS.md / CLAUDE.md 里，不要写进这份全局文件。

## 回复协议

- 执行前先把用户本轮诉求梳理成更精准、更利于执行的任务描述，再动手。
- 执行后的最终回复固定三段式：
  1. 有序列表汇总执行结果（容易理解的方式）。
  2. 阶段进度：阶段目标 + 百分比 + 尚未完成的有序列表。
  3. 下一步建议及原因。

## 上下文压缩后的动作

- 每次触发上下文压缩（`/compact`，包括因过长自动触发的压缩）后，回顾被压缩的对话内容，把新观察到的协作方法论要点合并更新进 `~/.claude/skills/bigfu-working-style/SKILL.md`（补充或改写已有条目，不新建重复文件，不整体覆盖）。

## 工作方法论

- 详细内容维护在全局 skill `~/.claude/skills/bigfu-working-style/SKILL.md`，每次对话都应参考，核心要点：证据优先拒绝空口承诺、外部风险/审查报告要举证核验、先做局部可验证的小优化、阶段目标只能由用户裁定、直接的自我批评不找借口、短指令即刻执行、文档要与实现同步不留只存在于对话记忆里的知识。

## 分层说明

- 全局（跨项目）：本文件 + `~/.claude/skills/`。
- 项目专属：各项目根目录的 `AGENTS.md`/`CLAUDE.md`，以及该项目的持久化 memory 目录（如 `~/.claude/projects/<project>/memory/`）。
- 两者冲突时，项目专属规则在该项目内优先；全局规则是缺省行为准则。
